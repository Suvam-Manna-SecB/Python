# Write a program in python to convert temperature from celcius to fahrenheit.

tempC = float(input('Enter Temperature in Celcius: '))
tempF = 1.8 * tempC + 32
print('Temperature in Fahrenheit: ', round(tempF,2))
